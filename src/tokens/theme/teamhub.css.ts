import { createThemeVars } from './creator';
import { colorLight } from '../../../shared/tokens/colorLight';
import { colorDark } from '../../../shared/tokens/colorDark';

const theme = createThemeVars(colorLight, colorDark);

export default theme;
