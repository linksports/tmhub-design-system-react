import { Meta, Story, Canvas, Controls } from "@storybook/blocks";
import PropsTable from "../../../.storybook/propertiesTable";
import * as TableStories from "./Table.stories";

<Meta of={TableStories} />

# Table

テーブルコンポーネント。構造は `<Table>`, `<Thead>`, `<Tbody>`, `<Tr>`, `<Th>`, `<Td>` のようにネストして利用します。

---

## API Reference

### `<Table>`

<PropsTable
  props={[
    ["columnNum*", "number", "列数（固定列計算に使用）"],
    [
      "fixedColumn",
      "{ left?: number; right?: number }",
      "固定列（左・右）の数",
    ],
    [
      "...divProps",
      'React.ComponentProps<"div">',
      "外枠divに渡すprops（styleなど）",
    ],
  ]}
/>

---

### `<Th>`

<PropsTable
  props={[
    ["index*", "number", "列のインデックス"],
    ["textAlign", '"left" | "center"', "テキストの横位置"],
    ["verticalWriting", "boolean", "縦書き表示"],
    ["onSort", '(direction?: "asc" | "desc") => void', "ソート実行関数"],
    ["children", "string | number", "セル表示テキスト"],
    ["...thProps", 'React.ComponentProps<"th">', "その他HTML th属性"],
  ]}
/>

---

### `<Td>`

<PropsTable
  props={[
    ["index*", "number", "列のインデックス"],
    [
      "textProps",
      '{ textAlign?: "left" | "center"; verticalWriting?: boolean; ...pProps }',
      "中の<p>要素に適用するProps",
    ],
    ["...tdProps", 'React.ComponentProps<"td">', "その他HTML td属性"],
  ]}
/>

---

## Playground（Basic）

<Canvas of={TableStories.Default} />

---

## Sort 対応

<Canvas of={TableStories.SortTable} />

---

## 固定列

<Canvas of={TableStories.FixedColumns} />

---

## 非テキスト行（アイコン + 複数要素）

<Canvas of={TableStories.RowContainsNonText} />
